bash ./config/init_config.sh

mkdir data && cd data

mkdir imageNet && cd imageNet

unzip ../Image.zip

mkdir hnsw_maintenance && mkdir hnsw_prime && cd ..
# 还要自己生成top-10

mkdir sift && cd sift

mkdir hnsw_maintenance && mkdir hnsw_prime && cd ..

mkdir gist && cd gist

mkdir hnsw_maintenance && mkdir hnsw_prime && cd ..

